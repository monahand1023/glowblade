# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# Local secrets (GEMINI_API_KEY / GOOGLE_API_KEY are read from the
# environment, never from a file -- this is a guard against ever adding one).
.env
.env.*

# Default CLI output path (src/lightsaber_fx/cli.py --output default);
# dropped into the working tree when `lightsaber-fx run` is invoked from repo root.
final.mp4

# Isolated worktrees for in-progress feature work
.worktrees/

.DS_Store
